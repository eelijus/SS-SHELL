#-------------- ERROR ---------------#

#-------------- CORRECTION ---------------#

[sean]
* ft_getenv를 생성
   기존의 getenv() 사용으로 인한 달러 변환 문제 해결

* ft_unset이 실행될 프로세스를 수정
   ft_unset으로 인한 env 변경이 정상적으로 작동되게 함.
   
 * echo $? ada
    -> ls ada 이런식으로 나오도록 수정함

* export a-a를 하는 경우 제대로 종료되게 함.

* 정확한 exit status 구현을 위해 print_error 코드를 수정하였으며 ft_export와 ft_unset의 print_error 함수 인자값도 수정함.
#-------------- DISCUSSION ---------------#

* ft_exit는 터미널에 exit라고 입력했을 때만 사용하고 나머지는 그냥 exit()를 사용하였다.
    -> exit가 사용가능 함수라서 그냥 ft_exit를 안쓰고 exit()를 사용했는데 이 부분을 ft_exit로 바꿔야 할지.

#-------------- NEXT JOBS ---------------#
   
* "<<" heredocs

* norminette check
